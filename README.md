# BBHS-Day-Tracker
Our school has a super-duper weird time table system, so I built a tracking server that will return the correct day of the cycle.

To use it, simply compile the typescript files, and run the server file (that's /dist/index.js). View at http://127.0.0.1:5492
